{
  "name": "baymax",
  "version": "0.1.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "node scripts/link-client-hoisted-deps.cjs",
    "start:client": "npm --prefix client start",
    "start:server": "npm --prefix server start",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "author": "beniciosamuel",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.6"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "nodemon": "^3.1.14"
  }
}
